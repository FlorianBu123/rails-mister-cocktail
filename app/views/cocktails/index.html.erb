<h1>My Cocktails</h1>
<ul>
<% @cocktails.each do |cocktail| %>
  <li><%= link_to cocktail.name, cocktail %></li>
  <div>
<%= cl_image_tag cocktail.photo.key, height: 75, width: 100, crop: :fill %>
</div>
<% end %>
</ul>


<%= link_to 'Create a new cocktail', new_cocktail_path %>